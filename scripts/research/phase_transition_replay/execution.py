"""Executable-price contract (BBO staleness vs BOUNDED_TICK_PROXY) — skeletal."""


def executable_prices(quotes, decision_ts, staleness_bounds, adverse_ticks=1):
    raise NotImplementedError("execution.executable_prices: BBO within bounds; tick-only -> BOUNDED_TICK_PROXY")
