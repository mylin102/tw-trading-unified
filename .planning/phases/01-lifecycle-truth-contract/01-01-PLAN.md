---
phase: 01-lifecycle-truth-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/order_management/order.py
  - core/order_management/order_fill.py
  - core/order_management/__init__.py
  - tests/test_order_lifecycle/test_contract_traceability.py
  - tests/test_order_lifecycle/test_order_export_dashboard.py
autonomous: true
requirements:
  - EXEC-01
must_haves:
  truths:
    - "A lifecycle record can be traced from local intent_id to local order_id to one or more fill_id values."
    - "Broker identifiers (broker_order_id/seqno/ordno/exchange_fill_id) are attached without breaking existing orders.json readers."
    - "Order export and import keep the traceability fields intact after JSON round-trip."
  artifacts:
    - path: core/order_management/order.py
      provides: "Order contract with traceability identifiers and compatibility serialization"
      contains: "intent_id"
    - path: core/order_management/order_fill.py
      provides: "Per-fill contract with broker fill identifiers"
      contains: "exchange_fill_id"
    - path: tests/test_order_lifecycle/test_contract_traceability.py
      provides: "Regression coverage for intent/order/fill linkage"
      contains: "test_order_round_trip_preserves_traceability"
  key_links:
    - from: core/order_management/order.py
      to: core/order_management/order_fill.py
      via: "fills list serialized through to_dict()/from_dict()"
      pattern: "fills"
    - from: core/order_management/order.py
      to: ui/dashboard.py
      via: "legacy-compatible export keys remain present"
      pattern: "exchange_order_id"
---

<objective>
Define the shared lifecycle identifiers and serialization contract so futures/options paper/live paths can trace intent → order → deal without breaking existing `*_orders.json` consumers.

Purpose: Deliver EXEC-01 first so later state and position changes build on one stable truth model.
Output: Expanded order/fill contracts plus regression tests that lock traceability and JSON compatibility.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lifecycle-truth-contract/01-RESEARCH.md
@.planning/phases/01-lifecycle-truth-contract/01-VALIDATION.md
@RULES.md
@docs/shioaji_訂單生命週期.md
@docs/example_order_manager.py
@core/order_management/order.py
@core/order_management/order_fill.py
@tests/test_order_lifecycle/test_order_export_dashboard.py

<interfaces>
From core/order_management/order.py:
```python
class OrderStatus(Enum):
    PENDING_SUBMIT = "pending_submit"
    PRE_SUBMITTED = "pre_submitted"
    SUBMITTED = "submitted"
    PARTIAL_FILLED = "partial_filled"
    FILLED = "filled"
    CANCELLED = "cancelled"
    REJECTED = "rejected"
    EXPIRED = "expired"
```

```python
class Order:
    def to_dict(self) -> Dict[str, Any]: ...
    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "Order": ...
```

From core/order_management/order_fill.py:
```python
class OrderFill:
    def to_dict(self) -> dict: ...
    @classmethod
    def from_dict(cls, data: dict) -> "OrderFill": ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 traceability regression coverage</name>
  <read_first>
    - tests/test_order_lifecycle/test_order_export_dashboard.py
    - core/order_management/order.py
    - core/order_management/order_fill.py
    - docs/example_order_manager.py
    - .planning/phases/01-lifecycle-truth-contract/01-RESEARCH.md
  </read_first>
  <files>tests/test_order_lifecycle/test_contract_traceability.py, tests/test_order_lifecycle/test_order_export_dashboard.py</files>
  <behavior>
    - Test 1: an `Order` with `intent_id`, broker order IDs, and nested fills round-trips through `to_dict()`/`from_dict()` without losing linkage.
    - Test 2: `Order.to_dict()` still emits legacy dashboard fields such as `exchange_order_id`, `status`, `filled_quantity`, and timestamps.
    - Test 3: `OrderFill.to_dict()` exposes `fill_id`, `order_id`, and broker fill identifiers needed for future deal reconciliation.
  </behavior>
  <action>Create `tests/test_order_lifecycle/test_contract_traceability.py` with focused regressions for D-implicit constraints from the phase prompt: traceable IDs, compatibility with existing `*_orders.json`, and no loss of legacy export fields. Update `tests/test_order_lifecycle/test_order_export_dashboard.py` to assert the new traceability keys are present alongside existing dashboard keys. Use exact field names `intent_id`, `order_id`, `fill_id`, `broker_order_id`, `seqno`, `ordno`, and `exchange_fill_id`; do not rename existing `exchange_order_id` because dashboard compatibility must remain intact.</action>
  <acceptance_criteria>
    - `grep -n "test_order_round_trip_preserves_traceability" tests/test_order_lifecycle/test_contract_traceability.py` returns a match.
    - `grep -n "intent_id" tests/test_order_lifecycle/test_order_export_dashboard.py` returns at least one assertion.
    - `python3 -m pytest tests/test_order_lifecycle/test_contract_traceability.py tests/test_order_lifecycle/test_order_export_dashboard.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_contract_traceability.py tests/test_order_lifecycle/test_order_export_dashboard.py --collect-only -q</automated>
  </verify>
  <done>Wave 0 tests exist, are syntactically valid, and explicitly lock the required traceability fields before contract changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the shared order and fill contracts for linked lifecycle IDs</name>
  <read_first>
    - core/order_management/order.py
    - core/order_management/order_fill.py
    - core/order_management/__init__.py
    - docs/example_order_manager.py
    - docs/shioaji_訂單生命週期.md
    - tests/test_order_lifecycle/test_contract_traceability.py
  </read_first>
  <files>core/order_management/order.py, core/order_management/order_fill.py, core/order_management/__init__.py</files>
  <action>Implement EXEC-01 by extending `Order` with explicit traceability fields: `intent_id`, `broker_order_id`, `seqno`, `ordno`, `fills: List[OrderFill]`, and optional `raw_events: List[Dict[str, Any]]`. Keep `exchange_order_id` as a compatibility alias populated from `broker_order_id`/`ordno` so current dashboard JSON remains readable. Extend `OrderFill` with `fill_id`, `exchange_fill_id`, optional `broker_trade_id`, optional `exchange_seq`, and ISO-safe serialization. Update `Order.to_dict()`/`from_dict()` to include nested `fills` and all new IDs without removing any existing keys. Export the updated types through `core/order_management/__init__.py`. Do not introduce new external dependencies and do not change dashboard-facing status strings.</action>
  <acceptance_criteria>
    - `grep -n "intent_id" core/order_management/order.py` returns a field definition or assignment.
    - `grep -n "exchange_fill_id" core/order_management/order_fill.py` returns a field definition or serialization entry.
    - `grep -n "\"fills\"" core/order_management/order.py` returns serialization logic in both `to_dict` and `from_dict`.
    - `python3 -m pytest tests/test_order_lifecycle/test_contract_traceability.py tests/test_order_lifecycle/test_order_export_dashboard.py -v` passes.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_contract_traceability.py tests/test_order_lifecycle/test_order_export_dashboard.py -v</automated>
  </verify>
  <done>`Order` and `OrderFill` can carry linked local and broker identifiers end-to-end, and existing export/dashboard tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy/runtime → lifecycle contract | Untrusted runtime data becomes persisted lifecycle identifiers and export rows |
| lifecycle contract → dashboard JSON | Serialized records cross into operator-visible read models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `core/order_management/order.py` serialization | mitigate | Preserve explicit typed fields and round-trip tests so IDs cannot be silently dropped or overwritten during export/import |
| T-01-02 | I | `*_orders.json` compatibility surface | mitigate | Keep legacy keys such as `exchange_order_id`, `status`, `filled_quantity`, and timestamps while adding new traceability keys |
| T-01-03 | R | lifecycle trace linkage | mitigate | Persist local `intent_id`/`order_id` and per-fill identifiers so later audits can prove which broker records belong to each strategy intent |
</threat_model>

<verification>
Run the targeted Wave 0 traceability suite, then rerun dashboard export compatibility tests:
- `python3 -m pytest tests/test_order_lifecycle/test_contract_traceability.py tests/test_order_lifecycle/test_order_export_dashboard.py -v`
</verification>

<success_criteria>
- `Order.to_dict()` and `Order.from_dict()` preserve intent/order/fill linkage.
- `OrderFill` serializes broker fill identifiers needed for later reconciliation.
- Existing dashboard export fields remain present and regression-locked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-lifecycle-truth-contract/01-lifecycle-truth-contract-01-SUMMARY.md`
</output>
