---
phase: 01-lifecycle-truth-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - core/order_management/order_manager.py
  - strategies/options/live_options_squeeze_monitor.py
  - tests/test_order_lifecycle/test_order_manager.py
  - tests/test_order_lifecycle/test_order_state_vs_deal_state.py
autonomous: true
requirements:
  - EXEC-02
must_haves:
  truths:
    - "Accepted/submitted, partial fill, full fill, cancel, and reject remain distinct lifecycle states."
    - "Order-status updates do not fabricate fills, and deal updates do not erase cancel/reject history."
    - "Options live callbacks preserve non-deal order truth instead of dropping it."
  artifacts:
    - path: core/order_management/order_manager.py
      provides: "Separate order-update and deal-apply flows"
      contains: "apply_order_update"
    - path: strategies/options/live_options_squeeze_monitor.py
      provides: "Options callback bridge for both order and deal events"
      contains: "on_order_event"
    - path: tests/test_order_lifecycle/test_order_state_vs_deal_state.py
      provides: "Regression coverage for state separation"
      contains: "test_order_update_does_not_create_fill"
  key_links:
    - from: strategies/options/live_options_squeeze_monitor.py
      to: core/order_management/order_manager.py
      via: "on_order_event forwarding order and deal callbacks"
      pattern: "apply_order_update|apply_deal_fill"
    - from: core/order_management/order_manager.py
      to: core/order_management/order.py
      via: "state transitions update order status without mutating fills list"
      pattern: "status"
---

<objective>
Separate order-state transitions from confirmed deal truth so the shared manager and live options callback path can distinguish accepted, partial, filled, cancel, and reject outcomes without collapse.

Purpose: Deliver EXEC-02 before futures position logic consumes lifecycle events.
Output: New order/deal update APIs in the shared manager, live options callback wiring, and regression tests proving the states stay separate.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lifecycle-truth-contract/01-RESEARCH.md
@.planning/phases/01-lifecycle-truth-contract/01-VALIDATION.md
@RULES.md
@docs/shioaji_訂單生命週期.md
@core/order_management/order_manager.py
@core/order_management/order.py
@strategies/options/live_options_squeeze_monitor.py
@tests/test_order_lifecycle/test_order_manager.py

<interfaces>
From core/order_management/order_manager.py:
```python
class OrderEvent:
    order_id: str
    status: OrderStatus
    symbol: str
    side: OrderSide
    fill_price: Optional[float] = None
    fill_qty: int = 0
    reason: str = ""
```

```python
class OrderManager:
    def create_order(...): ...
    def submit(self, order: Order, exchange_ordno: Optional[str] = None) -> bool: ...
    def on_fill(self, order_id: str, fill_price: float, fill_qty: int, partial: bool, ...) -> None: ...
    def cancel(self, order_id: str, reason: str = "") -> bool: ...
    def reject(self, order_id: str, reason: str) -> None: ...
```

From strategies/options/live_options_squeeze_monitor.py:
```python
def on_order_event(self, stat, msg):
    is_deal = stat in (sj.constant.OrderState.FuturesDeal, sj.constant.OrderState.StockDeal)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 tests for order-state versus deal-state separation</name>
  <read_first>
    - tests/test_order_lifecycle/test_order_manager.py
    - core/order_management/order_manager.py
    - strategies/options/live_options_squeeze_monitor.py
    - docs/shioaji_訂單生命週期.md
    - .planning/phases/01-lifecycle-truth-contract/01-VALIDATION.md
  </read_first>
  <files>tests/test_order_lifecycle/test_order_manager.py, tests/test_order_lifecycle/test_order_state_vs_deal_state.py</files>
  <behavior>
    - Test 1: broker order updates set submitted/cancelled/rejected states without creating `fills`.
    - Test 2: deal updates append `OrderFill` records and drive `partial_filled`/`filled` based on remaining quantity.
    - Test 3: a cancelled order with prior partial fills keeps both the partial fill history and terminal cancelled state.
    - Test 4: externally placed live orders can attach broker IDs after submit without requiring `broker_adapter.place_order(order)` compatibility.
  </behavior>
  <action>Create `tests/test_order_lifecycle/test_order_state_vs_deal_state.py` and extend `tests/test_order_lifecycle/test_order_manager.py` with concrete coverage for new manager APIs `attach_submission`, `apply_order_update`, and `apply_deal_fill`. Use exact Shioaji-facing state inputs `PendingSubmit`, `Submitted`, `PartFilled`, `Filled`, `Cancelled`, and `Failed`; assert internal outputs remain `pending_submit`, `submitted`, `partial_filled`, `filled`, `cancelled`, and `rejected` respectively. Include one regression proving a cancel after partial fill keeps `filled_quantity` and `fills` unchanged while terminal status becomes `cancelled`.</action>
  <acceptance_criteria>
    - `grep -n "test_order_update_does_not_create_fill" tests/test_order_lifecycle/test_order_state_vs_deal_state.py` returns a match.
    - `grep -n "attach_submission" tests/test_order_lifecycle/test_order_manager.py` returns at least one new assertion block.
    - `python3 -m pytest tests/test_order_lifecycle/test_order_manager.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_order_manager.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py --collect-only -q</automated>
  </verify>
  <done>The manager and callback separation behaviors are locked in tests before runtime lifecycle code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement separate order-update and deal-apply flows in the shared manager and options callback bridge</name>
  <read_first>
    - core/order_management/order_manager.py
    - strategies/options/live_options_squeeze_monitor.py
    - tests/test_order_lifecycle/test_order_manager.py
    - tests/test_order_lifecycle/test_order_state_vs_deal_state.py
    - docs/example_order_manager.py
  </read_first>
  <files>core/order_management/order_manager.py, strategies/options/live_options_squeeze_monitor.py</files>
  <action>Implement EXEC-02 by extending `OrderManager` with explicit lifecycle APIs: `attach_submission(order_id, *, broker_trade=None, broker_order_id=None, seqno=None, ordno=None, raw_status=None)`, `apply_order_update(order_id, *, raw_status, reason="", raw_payload=None, broker_order_id=None, seqno=None, ordno=None)`, and `apply_deal_fill(order_id, *, fill_price, fill_qty, fill_time=None, exchange_fill_id=None, broker_trade_id=None, exchange_seq=None, commission=0.0, tax=0.0, raw_payload=None)`. Keep existing `submit()`/`on_fill()`/`cancel()` for compatibility by routing them through the new helpers. Preserve existing callback names `on_fill`, `on_cancel`, and `on_reject`; add a new non-breaking `on_status_change` callback for accepted/submitted/pre-submitted transitions if helpful. Update `ShioajiOptionsSmartMonitor.on_order_event()` so order-status callbacks no longer return early: deal events must call `apply_deal_fill`, while order callbacks must call `apply_order_update` and still save the orders file. Do not mutate options `position` on pure order-status callbacks; only deal events may affect holdings.</action>
  <acceptance_criteria>
    - `grep -n "def attach_submission" core/order_management/order_manager.py` returns a function definition.
    - `grep -n "def apply_order_update" core/order_management/order_manager.py` returns a function definition.
    - `grep -n "def apply_deal_fill" core/order_management/order_manager.py` returns a function definition.
    - `grep -n "apply_order_update" strategies/options/live_options_squeeze_monitor.py` returns at least one callback forwarding call.
    - `python3 -m pytest tests/test_order_lifecycle/test_order_manager.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py -v` passes.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_order_manager.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py -v</automated>
  </verify>
  <done>The shared manager can track accepted/partial/filled/cancelled/rejected as separate lifecycle truths, and options live no longer discards non-deal callbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker callback payload → order manager | Untrusted Shioaji status/deal payloads drive internal state transitions |
| options callback bridge → dashboard export | Lifecycle status becomes operator-visible JSON read models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `core/order_management/order_manager.py` state transitions | mitigate | Separate `apply_order_update` from `apply_deal_fill` so status callbacks cannot fabricate fills or mutate quantity |
| T-01-05 | R | options order callback handling | mitigate | Preserve order callbacks in `ShioajiOptionsSmartMonitor.on_order_event()` instead of silently dropping accepted/cancel/reject events |
| T-01-06 | I | partial-fill and cancel interplay | mitigate | Keep `fills`, `filled_quantity`, and remaining quantity immutable except through deal application; cancellation only changes terminal state and reason |
</threat_model>

<verification>
Run the focused manager suite after implementation:
- `python3 -m pytest tests/test_order_lifecycle/test_order_manager.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py -v`
</verification>

<success_criteria>
- Shared order manager exposes separate order-state and deal-state entry points.
- Live options callback flow preserves order-status truth without mutating holdings.
- Partial fill, full fill, cancel, and reject are all regression-locked as distinct outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-lifecycle-truth-contract/01-lifecycle-truth-contract-02-SUMMARY.md`
</output>
