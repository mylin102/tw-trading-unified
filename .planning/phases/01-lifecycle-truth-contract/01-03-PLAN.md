---
phase: 01-lifecycle-truth-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - strategies/futures/monitor.py
  - tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py
  - tests/test_order_lifecycle/test_system_lifecycle.py
autonomous: true
requirements:
  - EXEC-03
must_haves:
  truths:
    - "Futures position and cost basis do not change when a live/paper order is only submitted or pending."
    - "Confirmed deal events, including partial fills, are the only path that mutates `PaperTrader.position` and cost basis."
    - "Fee-inclusive PnL remains intact because exits still flow through `PaperTrader.execute_signal` only after confirmed deals."
  artifacts:
    - path: strategies/futures/monitor.py
      provides: "Deal-only futures position applier and safe live-order submission path"
      contains: "_apply_confirmed_futures_deal"
    - path: tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py
      provides: "Regression coverage blocking optimistic submit mutation"
      contains: "test_live_submit_does_not_change_position"
    - path: tests/test_order_lifecycle/test_system_lifecycle.py
      provides: "Lifecycle integration proof for partial deal application"
      contains: "confirmed_deal"
  key_links:
    - from: strategies/futures/monitor.py
      to: strategies/futures/squeeze_futures/engine/simulator.py
      via: "confirmed deal handler calling PaperTrader.execute_signal"
      pattern: "execute_signal"
    - from: strategies/futures/monitor.py
      to: core/order_management/order_manager.py
      via: "attach_submission/apply_order_update/apply_deal_fill bridge"
      pattern: "attach_submission|apply_order_update|apply_deal_fill"
---

<objective>
Anchor futures position and cost-basis changes to confirmed deal handling only, keeping `PaperTrader.position` as the truth anchor and preventing optimistic mutation after submit.

Purpose: Deliver EXEC-03 safely without destabilizing `main.py` or the 8500 dashboard.
Output: Futures monitor helpers that submit live orders into lifecycle tracking without moving position until deal callbacks arrive, plus regression tests for partial and full confirmed fills.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lifecycle-truth-contract/01-RESEARCH.md
@.planning/phases/01-lifecycle-truth-contract/01-VALIDATION.md
@RULES.md
@strategies/futures/monitor.py
@strategies/futures/squeeze_futures/engine/simulator.py
@core/order_management/order_manager.py
@tests/test_order_lifecycle/test_system_lifecycle.py

<interfaces>
From strategies/futures/squeeze_futures/engine/simulator.py:
```python
def execute_signal(self, signal: str, price: float, timestamp: datetime, lots=1, max_lots=1, stop_loss=None, break_even_trigger=None, trail_points=None, exit_reason: str = None):
    ...
```

Key behavior from RULES.md:
```text
PaperTrader.position is the ONLY truth for futures position
Side effects must happen AFTER the core operation succeeds
PnL must include broker_fee + exchange_fee + tax
```

From strategies/futures/monitor.py:
```python
if self._use_order_manager and self.order_mgr and signal in ("BUY", "SELL"):
    return self._submit_order_via_manager(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 regression coverage for deal-only position mutation</name>
  <read_first>
    - tests/test_order_lifecycle/test_system_lifecycle.py
    - strategies/futures/monitor.py
    - strategies/futures/squeeze_futures/engine/simulator.py
    - RULES.md
    - .planning/phases/01-lifecycle-truth-contract/01-VALIDATION.md
  </read_first>
  <files>tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py, tests/test_order_lifecycle/test_system_lifecycle.py</files>
  <behavior>
    - Test 1: live futures submit creates/attaches a lifecycle order but leaves `PaperTrader.position` unchanged until a confirmed deal arrives.
    - Test 2: a partial confirmed deal updates `PaperTrader.position` by the filled quantity only.
    - Test 3: exit-side confirmed deals reduce or zero position through `PaperTrader.execute_signal`, preserving fee/tax-inclusive PnL math.
    - Test 4: submit-only paths do not call `save_trade` or append synthetic filled lifecycle rows.
  </behavior>
  <action>Create `tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py` and extend `tests/test_order_lifecycle/test_system_lifecycle.py` with focused futures monitor regressions. Use a fake live trade object plus mocked `PaperTrader`, `save_trade`, and order manager callbacks so the tests prove: (a) submit does not move position, (b) `PARTIAL_FILLED` deal events apply incremental lots, and (c) `FILLED` exit deals finalize position/PnL only after confirmed deal processing. Name the new tests with explicit behavior such as `test_live_submit_does_not_change_position` and `test_partial_confirmed_deal_updates_position_incrementally`.</action>
  <acceptance_criteria>
    - `grep -n "test_live_submit_does_not_change_position" tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py` returns a match.
    - `grep -n "partial_confirmed_deal" tests/test_order_lifecycle/test_system_lifecycle.py` returns at least one new test name or assertion block.
    - `python3 -m pytest tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py tests/test_order_lifecycle/test_system_lifecycle.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py tests/test_order_lifecycle/test_system_lifecycle.py --collect-only -q</automated>
  </verify>
  <done>Regression coverage exists for the exact financial-safety invariant that positions change only after confirmed deal events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor FuturesMonitor to apply positions only from confirmed deals</name>
  <read_first>
    - strategies/futures/monitor.py
    - core/order_management/order_manager.py
    - strategies/futures/squeeze_futures/engine/simulator.py
    - tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py
    - RULES.md
  </read_first>
  <files>strategies/futures/monitor.py</files>
  <action>Implement EXEC-03 per the phase constraints. In `FuturesMonitor`, add a dedicated helper such as `_apply_confirmed_futures_deal(event)` that is the only place allowed to call `self.trader.execute_signal(...)` for lifecycle-driven fills. Update `_wire_order_callbacks()` so `PARTIAL_FILLED` and `FILLED` deal events both flow through that helper using `event.fill_qty`; do not wait only for terminal fills. Introduce per-order pending metadata (for example `_pending_lifecycle_orders[order_id] = {signal, stop_loss, reason, ts, lots}`) so submit-time context is available when deal callbacks arrive. In the live submission path, when `self._use_order_manager` is enabled, place the broker order with `self.client.place_order(...)`, create/lookup the local lifecycle order, call `self.order_mgr.attach_submission(...)`, and return early without calling `self.trader.execute_signal`, `save_trade`, or `_append_filled_lifecycle_order`. For paper mode, keep the existing order-manager simulation flow but remove any optimistic submit-side position mutation so paper and live both obey the same deal-only invariant. Preserve existing fee/tax-inclusive exit behavior by routing confirmed exits back through `PaperTrader.execute_signal("EXIT"|"PARTIAL_EXIT", ...)` instead of bypassing the simulator math.</action>
  <acceptance_criteria>
    - `grep -n "def _apply_confirmed_futures_deal" strategies/futures/monitor.py` returns a function definition.
    - `grep -n "attach_submission" strategies/futures/monitor.py` returns at least one live submit integration call.
    - `grep -n "PARTIAL_FILLED" strategies/futures/monitor.py` returns callback handling that applies partial fills.
    - `python3 -m pytest tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py tests/test_order_lifecycle/test_system_lifecycle.py -v` passes.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py tests/test_order_lifecycle/test_system_lifecycle.py -v</automated>
  </verify>
  <done>Futures submit paths stop mutating `PaperTrader.position` optimistically, while confirmed deal events drive incremental and terminal position/cost updates with existing fee/tax logic intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| futures signal execution → broker submit path | Strategy intent becomes live broker order submission and pending lifecycle metadata |
| confirmed deal callback → `PaperTrader` | Broker-confirmed execution crosses into position truth and PnL calculation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `strategies/futures/monitor.py` submit path | mitigate | Return early after live submit + `attach_submission` so submit success cannot mutate `PaperTrader.position` or emit post-trade side effects |
| T-01-08 | I | confirmed deal applier | mitigate | Centralize all futures position changes in `_apply_confirmed_futures_deal()` and cover partial/full fills with targeted tests |
| T-01-09 | R | trade/PnL export sequencing | mitigate | Keep `save_trade` and lifecycle export only after `PaperTrader.execute_signal(...)` succeeds, preserving RULES.md side-effect ordering |
</threat_model>

<verification>
Run the targeted futures invariant suite:
- `python3 -m pytest tests/test_order_lifecycle/test_position_apply_on_confirmed_deal.py tests/test_order_lifecycle/test_system_lifecycle.py -v`
</verification>

<success_criteria>
- Live and paper futures submit paths no longer move `PaperTrader.position` before confirmed deals.
- Partial and full confirmed deals update position incrementally through one deal applier.
- Exit PnL still includes fees and tax because `PaperTrader.execute_signal` remains the calculation path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-lifecycle-truth-contract/01-lifecycle-truth-contract-03-SUMMARY.md`
</output>
