---
phase: 1000-implement-truthful-live-theta-execution-and-complex-order-or
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - strategies/options/options_engine/engine/broker_adapter.py
  - core/order_management/order_manager.py
  - strategies/options/live_options_squeeze_monitor.py
  - tests/test_combo_recovery.py
  - tests/test_order_lifecycle/test_order_state_vs_deal_state.py
autonomous: true
requirements:
  - RECN-01
  - RECN-02
  - RECN-03
  - EXEC-03
must_haves:
  truths:
    - "Combo fills and status changes are recovered from broker combo APIs before any ledger rebuild is trusted."
    - "Restart recovery restores pending/open/filled combo lifecycle truth without resubmitting theta orders."
    - "Local theta open/close state changes only after recovered or polled combo fill truth is applied."
    - "Audit history preserves why combo state came from callback, poll, or restart recovery."
  artifacts:
    - path: strategies/options/live_options_squeeze_monitor.py
      provides: "Combo reconciliation and startup recovery orchestration"
      contains: "list_combotrades"
    - path: core/order_management/order_manager.py
      provides: "Combo-aware recovery/update helpers that preserve audit source and raw payload"
      contains: "recover_from_api"
    - path: tests/test_combo_recovery.py
      provides: "Regression coverage for combo restart and no-ledger-first recovery"
      contains: "test_live_combo_recovery_prefers_broker_over_ledger"
  key_links:
    - from: strategies/options/live_options_squeeze_monitor.py
      to: strategies/options/options_engine/engine/broker_adapter.py
      via: "polling/recovery helpers call update_combostatus/list_combotrades"
      pattern: "update_combostatus|list_combotrades"
    - from: strategies/options/live_options_squeeze_monitor.py
      to: core/order_management/order_manager.py
      via: "combo broker status translates into apply_order_update/apply_deal_fill on the existing lifecycle order"
      pattern: "apply_order_update|apply_deal_fill"
---

<objective>
Make combo execution truthful after submission by reconciling fills/status directly from broker combo APIs during runtime and restart recovery, never from paper synthesis or premature ledger assumptions.

Purpose: Preserve broker-truth lifecycle semantics when combo callbacks are uncertain and restarts happen.
Output: Combo polling/recovery flow, combo-aware lifecycle reconciliation, and regression tests for restart/recovery behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1000-implement-truthful-live-theta-execution-and-complex-order-or/1000-RESEARCH.md
@strategies/options/live_options_squeeze_monitor.py
@core/order_management/order_manager.py
@strategies/options/options_engine/engine/broker_adapter.py
@tests/test_order_lifecycle/test_order_state_vs_deal_state.py

<interfaces>
From core/order_management/order_manager.py:
```python
def apply_order_update(self, order_id, *, raw_status, reason="", raw_payload=None, broker_order_id=None, seqno=None, ordno=None) -> Optional[Order]
def apply_deal_fill(self, order_id, *, deal_id=None, fill_price, fill_qty, fill_time=None, exchange_fill_id=None, broker_trade_id=None, exchange_seq=None, raw_payload=None, broker_order_id=None, ordno=None, source="", reason="") -> Optional[Order]
def recover_from_api(self, filled_trades=None, open_orders=None, source="", reason="") -> Dict[str, Any]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add combo recovery and reconciliation regressions</name>
  <files>tests/test_combo_recovery.py, tests/test_order_lifecycle/test_order_state_vs_deal_state.py</files>
  <behavior>
    - Test 1: startup recovery calls combo APIs before ledger rebuild and restores broker combo lifecycle rows when broker data exists.
    - Test 2: combo partial fill updates the existing lifecycle order without opening/closing theta state prematurely.
    - Test 3: combo cancel/reject paths preserve audit source/reason and do not fabricate fills.
    - Test 4: recovered combo fills reopen or close theta state exactly once and never resubmit the broker order.
  </behavior>
  <action>Create `tests/test_combo_recovery.py` for startup/runtime reconciliation and extend `tests/test_order_lifecycle/test_order_state_vs_deal_state.py` with combo partial-fill, cancel, reject, and recovery scenarios. Model combo status payloads using the research constraint that `ComboStatus.deals` is a dict of leg deal lists, then assert the system stores the full raw payload while mapping net lifecycle truth onto one combo order. Include one regression proving ledger rebuild is skipped when broker combo data is available.</action>
  <verify>
    <automated>python3 -m pytest tests/test_combo_recovery.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py -k "combo" -v</automated>
  </verify>
  <done>Failing tests exist for combo recovery ordering, combo status translation, and no-resubmit restart behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement combo polling/recovery and apply fill truth only from broker combo status</name>
  <files>strategies/options/options_engine/engine/broker_adapter.py, core/order_management/order_manager.py, strategies/options/live_options_squeeze_monitor.py</files>
  <action>Implement combo reconciliation around the verified broker APIs instead of relying on ordinary order callbacks. Add adapter helpers that wrap `update_combostatus()` and `list_combotrades()`. In the monitor, add a runtime helper (for example `_reconcile_theta_combo_orders`) and startup recovery path that: (1) loads combo statuses/trades before any ledger fallback, (2) resolves the matching lifecycle order via stored broker combo IDs, (3) maps combo status into `apply_order_update(...)`, (4) applies confirmed fills through `apply_deal_fill(...)` only once per recovered combo-deal identity, and (5) only then opens/closes theta local state, ledger rows, and saved order exports. Extend `OrderManager.recover_from_api()` or adjacent helpers so combo recovery records audit `source`/`reason` and preserves combo raw payloads, but do not invent fake leg lifecycle orders. If no broker combo data exists, allow current ledger fallback only as a visibly degraded surface for later dashboard labeling; do not let ledger recovery supersede broker combo truth.</action>
  <verify>
    <automated>python3 -m pytest tests/test_combo_recovery.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py -k "combo" -v</automated>
    <automated>python3 -m pytest tests/ -v</automated>
  </verify>
  <done>Combo state is reconciled from broker APIs during runtime and restart, with lifecycle/order/position updates driven only by broker-confirmed combo fill truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker combo API → local lifecycle state | Remote combo status/deal payloads rebuild local order truth |
| recovery flow → local strategy state | Restart reconciliation can incorrectly duplicate or mutate local theta state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1000-07 | T | combo recovery ordering | mitigate | Prefer broker combo APIs before ledger fallback and regression-lock that precedence |
| T-1000-08 | R | combo restart recovery | mitigate | Persist recovery source/reason/raw payload in lifecycle audit so operators can see what rebuilt state after restart |
| T-1000-09 | D | duplicate fill/replay | mitigate | Deduplicate combo deal application by recovered broker combo-deal identity before mutating theta state or ledger |
</threat_model>

<verification>
- `python3 -m pytest tests/test_combo_recovery.py tests/test_order_lifecycle/test_order_state_vs_deal_state.py -k "combo" -v`
- `python3 -m pytest tests/ -v`
</verification>

<success_criteria>
- Broker combo APIs drive runtime and restart reconciliation.
- Combo recovery never resubmits or double-applies fills.
- Ledger fallback remains last-resort visibility, not primary truth, for live combo orders.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-implement-truthful-live-theta-execution-and-complex-order-or/1000-implement-truthful-live-theta-execution-and-complex-order-or-03-SUMMARY.md`
</output>
