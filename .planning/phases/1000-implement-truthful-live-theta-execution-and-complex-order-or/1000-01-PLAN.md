---
phase: 1000-implement-truthful-live-theta-execution-and-complex-order-or
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - strategies/options/options_engine/engine/broker_adapter.py
  - core/order_management/order.py
  - core/order_management/order_manager.py
  - tests/test_broker_combo_adapter.py
  - tests/test_order_lifecycle/test_order_manager.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
must_haves:
  truths:
    - "One broker combo order becomes one lifecycle order instead of two fake single-leg fills."
    - "The lifecycle record preserves broker combo identifiers, leg metadata, and truth source without breaking current exports."
    - "The broker adapter exposes explicit Shioaji combo APIs instead of reusing single-leg order helpers."
  artifacts:
    - path: strategies/options/options_engine/engine/broker_adapter.py
      provides: "Combo-order adapter methods using Shioaji ComboContract/ComboOrder APIs"
      contains: "place_comboorder"
    - path: core/order_management/order.py
      provides: "Order contract with combo metadata and truth-source export fields"
      contains: "truth_source"
    - path: tests/test_broker_combo_adapter.py
      provides: "Regression coverage for 2-leg combo construction and broker calls"
      contains: "test_place_combo_entry_order_builds_two_leg_combo"
  key_links:
    - from: strategies/options/options_engine/engine/broker_adapter.py
      to: core/order_management/order_manager.py
      via: "combo submit returns identifiers that lifecycle attach_submission can persist"
      pattern: "ordno|seqno|place_comboorder"
    - from: core/order_management/order.py
      to: ui/dashboard.py
      via: "exported truth_source/combo_legs fields"
      pattern: "truth_source|combo_legs"
---

<objective>
Create the combo-order foundation the repo is missing so live theta spreads can use truthful Shioaji combo execution instead of pretending two-leg spreads are single-leg or paper orders.

Purpose: Establish the broker-truth contract before runtime/live-path changes depend on it.
Output: Combo-capable broker adapter methods, serialized combo lifecycle metadata, and targeted contract tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1000-implement-truthful-live-theta-execution-and-complex-order-or/1000-RESEARCH.md
@strategies/options/options_engine/engine/broker_adapter.py
@core/order_management/order.py
@core/order_management/order_manager.py

<interfaces>
From strategies/options/options_engine/engine/broker_adapter.py:
```python
class ShioajiBrokerAdapter:
    def place_entry_order(self, contract, quantity): ...
    def place_exit_order(self, contract, quantity, bid_price=None): ...
    def refresh_status(self, account=None): ...
```

From core/order_management/order_manager.py:
```python
def create_order(self, symbol, side, order_type, quantity, price=None, stop_price=None, strategy="", account="", comment="") -> Order
def attach_submission(self, order_id, *, broker_trade=None, broker_order_id=None, seqno=None, ordno=None, raw_status=None, source="", reason="") -> Optional[Order]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add combo adapter and lifecycle contract tests first</name>
  <files>tests/test_broker_combo_adapter.py, tests/test_order_lifecycle/test_order_manager.py</files>
  <behavior>
    - Test 1: a vertical spread builds exactly two combo legs and calls `place_comboorder()` with `ComboContract` + `ComboOrder`, not `place_order()`.
    - Test 2: adapter rejects combos whose leg count is not 2, per the installed Shioaji limitation.
    - Test 3: lifecycle orders can export/import `truth_source="broker_combo"` plus `combo_legs` metadata without losing existing order fields.
    - Test 4: combo cancel/status/list helpers call `cancel_comboorder()`, `update_combostatus()`, and `list_combotrades()` on the API surface.
  </behavior>
  <action>Create `tests/test_broker_combo_adapter.py` to lock the installed Shioaji combo API names from research: `ComboContract`, `ComboOrder`, `place_comboorder`, `cancel_comboorder`, `update_combostatus`, and `list_combotrades`. Extend `tests/test_order_lifecycle/test_order_manager.py` with serialization/round-trip coverage for a combo lifecycle order that exports `truth_source`, `combo_legs`, and broker IDs while preserving existing fields like `status`, `avg_fill_price`, and timestamps. Treat the broker-truth unit as one combo order, not two child lifecycle orders, per the phase constraint.</action>
  <verify>
    <automated>python3 -m pytest tests/test_broker_combo_adapter.py tests/test_order_lifecycle/test_order_manager.py -k "combo or truth_source" -v</automated>
  </verify>
  <done>Failing tests exist for combo API usage, 2-leg gating, and combo lifecycle metadata export before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Shioaji combo adapter methods and lifecycle metadata exports</name>
  <files>strategies/options/options_engine/engine/broker_adapter.py, core/order_management/order.py, core/order_management/order_manager.py</files>
  <action>Extend `ShioajiBrokerAdapter` with explicit combo helpers for tonight-safe 2-leg spreads only: build combo legs from contract/action pairs, create `ComboContract`, create `ComboOrder`, and expose submit/cancel/status/list helpers using the exact Shioaji method names verified in `1000-RESEARCH.md`. Do not introduce any `OptionComplexOrder` abstraction. Extend `Order` and `OrderManager.create_order()` to accept/export top-level combo metadata needed by later plans: `truth_source` (`broker_combo`, `paper_strategy`, `ledger_rebuilt`), `combo_legs` (leg code/action/ratio summary), and `combo_strategy`. Keep existing fields/backward compatibility intact. Preserve one lifecycle order per combo order and use `raw_events` for full broker payloads rather than inventing fake per-leg lifecycle orders.</action>
  <verify>
    <automated>python3 -m pytest tests/test_broker_combo_adapter.py tests/test_order_lifecycle/test_order_manager.py -k "combo or truth_source" -v</automated>
    <automated>python3 -m pytest tests/ -v</automated>
  </verify>
  <done>The adapter can submit/cancel/query 2-leg combos and lifecycle exports can carry combo truth metadata without breaking existing readers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy runtime → broker adapter | Local spread intent becomes live broker combo instructions |
| lifecycle export → dashboard/read models | Combo metadata becomes operator-visible truth labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1000-01 | T | `broker_adapter.py` combo construction | mitigate | Lock exact Shioaji combo API names and 2-leg restriction in tests so live code cannot silently fall back to single-leg order placement |
| T-1000-02 | I | `order.py` combo export surface | mitigate | Add explicit `truth_source`/`combo_legs` fields while preserving old export keys so UI truth is additive, not ambiguous |
| T-1000-03 | R | combo lifecycle identity | mitigate | Treat one combo order as one lifecycle order and persist broker IDs/raw payload on that record for later audit/recovery |
</threat_model>

<verification>
- `python3 -m pytest tests/test_broker_combo_adapter.py tests/test_order_lifecycle/test_order_manager.py -k "combo or truth_source" -v`
- `python3 -m pytest tests/ -v`
</verification>

<success_criteria>
- Combo orders use Shioaji combo APIs directly.
- Lifecycle exports preserve combo truth metadata.
- No implementation path reuses the single-leg order helper for vertical spreads.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-implement-truthful-live-theta-execution-and-complex-order-or/1000-implement-truthful-live-theta-execution-and-complex-order-or-01-SUMMARY.md`
</output>
