---
phase: 1000-implement-truthful-live-theta-execution-and-complex-order-or
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - strategies/options/live_options_squeeze_monitor.py
  - strategies/options/theta_gang.py
  - tests/test_theta_live_combo_execution.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - "Live theta vertical spreads submit real combo orders instead of paper lifecycle rows."
    - "Unsupported live theta strategies are explicitly rejected instead of silently simulating paper execution."
    - "Spread margin/cost pre-checks reflect combo max-loss reality instead of single-leg premium math."
    - "Local theta position state waits for broker-confirmed combo fills rather than opening optimistically on submit."
  artifacts:
    - path: strategies/options/live_options_squeeze_monitor.py
      provides: "Live theta combo submit/exit path with pending broker-truth state"
      contains: "pending_theta_combo"
    - path: strategies/options/theta_gang.py
      provides: "Spread validation and combo-aware risk helpers"
      contains: "bull_put_spread"
    - path: tests/test_theta_live_combo_execution.py
      provides: "Regression coverage for live combo submit and unsupported-strategy gating"
      contains: "test_live_vertical_theta_uses_combo_order"
  key_links:
    - from: strategies/options/live_options_squeeze_monitor.py
      to: strategies/options/options_engine/engine/broker_adapter.py
      via: "live theta entry/exit submit helpers call combo adapter methods"
      pattern: "place_combo"
    - from: strategies/options/live_options_squeeze_monitor.py
      to: strategies/options/theta_gang.py
      via: "validated 2-leg spread entry_info drives combo submit and risk checks"
      pattern: "strategy|max_loss|legs"
---

<objective>
Wire the live theta runtime to submit truthful broker combo orders for 2-leg verticals and to block every unsupported live theta strategy before it can fall back to paper behavior.

Purpose: Replace the current live-theta skip/paper mismatch with a broker-truth submit path that is tonight-safe.
Output: Live vertical combo entry/exit submit helpers, unsupported-strategy gating, combo-aware risk checks, and targeted regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1000-implement-truthful-live-theta-execution-and-complex-order-or/1000-RESEARCH.md
@.planning/debug/theta-dashboard-unrealized-pnl.md
@strategies/options/live_options_squeeze_monitor.py
@strategies/options/theta_gang.py
@strategies/options/options_engine/engine/broker_adapter.py

<interfaces>
From strategies/options/theta_gang.py:
```python
class SpreadPosition:
    strategy: str
    legs: List[SpreadLeg]
    net_credit: float
    max_loss: float
    quantity: int
```

From strategies/options/live_options_squeeze_monitor.py:
```python
def _record_paper_order(self, side_label, action, quantity, price, note="", strategy_override=None)
def _margin_sufficient(self)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add live-theta combo execution regressions before runtime changes</name>
  <files>tests/test_theta_live_combo_execution.py</files>
  <behavior>
    - Test 1: live `bull_put_spread` submits one combo lifecycle order and one broker combo order, never `_record_paper_order(...)`.
    - Test 2: live `bear_call_spread` exit reverses leg actions through a combo order and does not close local state until broker reconciliation confirms fills.
    - Test 3: live `iron_condor` and `short_strangle` emit an explicit unsupported warning/audit reason and do not place broker or paper orders.
    - Test 4: combo risk pre-check uses spread max-loss/wing-width semantics, not `entry_price * 50 * lots`.
  </behavior>
  <action>Create `tests/test_theta_live_combo_execution.py` with fake broker/order-manager collaborators that prove tonight-safe scope: only 2-leg vertical spreads (`bull_put_spread`, `bear_call_spread`) are eligible for live combo submission, unsupported live theta strategies are hard-stopped, and neither entry nor exit uses `_record_paper_order(...)` in live mode. Include assertions for a pending combo state object (for example `pending_theta_combo`) so the runtime does not call `ThetaGangManager.open_position()` or `close_position()` until combo fills are later confirmed.</action>
  <verify>
    <automated>python3 -m pytest tests/test_theta_live_combo_execution.py -v</automated>
  </verify>
  <done>Failing tests describe the exact live theta behaviors required by the phase constraints before runtime code changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement truthful live vertical combo submit, risk checks, and unsupported-strategy gates</name>
  <files>strategies/options/live_options_squeeze_monitor.py, strategies/options/theta_gang.py</files>
  <action>In `ThetaGangManager` and `ShioajiOptionsSmartMonitor`, add an explicit live-theta capability gate: only `bull_put_spread` and `bear_call_spread` with exactly two legs are live-capable tonight; `iron_condor` and `short_strangle` must log/audit a clear unsupported-live message and return without placing broker or paper orders. Replace the current live theta skip path with combo submission helpers that: (1) validate two legs plus one SELL/one BUY leg shape, (2) compute required risk from spread `max_loss`/wing width instead of single-leg premium logic, (3) create one lifecycle order with `symbol="TXO-COMBO"`, `truth_source="broker_combo"`, `combo_strategy`, and `combo_legs`, (4) call the new combo adapter submit method for entry/exit, and (5) store pending entry/exit metadata (e.g. `pending_theta_combo`) without mutating `self.position`, `self.active_side`, ledger rows, or dashboard-exported fill truth yet. Do not call `_record_paper_order(...)` anywhere on live theta entry/exit. Do not treat submit success as fill truth.</action>
  <verify>
    <automated>python3 -m pytest tests/test_theta_live_combo_execution.py -v</automated>
    <automated>python3 -m pytest tests/ -v</automated>
  </verify>
  <done>Live vertical theta spreads submit broker combo orders with pending truth only, while unsupported live theta strategies stop explicitly without paper fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy decision → live submit path | Theta strategy intent becomes real-money broker combo orders |
| live submit success → local position state | Untrusted submit acknowledgements could be mistaken for fills |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1000-04 | E | `live_options_squeeze_monitor.py` live theta branch | mitigate | Gate live theta to verified 2-leg verticals only and reject unsupported strategies explicitly |
| T-1000-05 | T | live theta state mutation on submit | mitigate | Store pending combo metadata only; do not open/close theta position or write ledger fills until combo reconciliation confirms execution |
| T-1000-06 | D | margin/risk pre-check | mitigate | Replace premium-only checks with spread max-loss/wing-width logic so live combo entries cannot understate capital at risk |
</threat_model>

<verification>
- `python3 -m pytest tests/test_theta_live_combo_execution.py -v`
- `python3 -m pytest tests/ -v`
</verification>

<success_criteria>
- Live vertical theta paths no longer call `_record_paper_order(...)`.
- Unsupported live theta strategies fail fast and visibly.
- Submit-time theta state remains pending until broker-confirmed fills arrive.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-implement-truthful-live-theta-execution-and-complex-order-or/1000-implement-truthful-live-theta-execution-and-complex-order-or-02-SUMMARY.md`
</output>
