---
phase: 1000-implement-truthful-live-theta-execution-and-complex-order-or
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - core/dashboard_positions.py
  - ui/dashboard.py
  - tests/test_dashboard_positions.py
  - tests/test_options_order_lifecycle_dashboard.py
  - tests/test_options_combo_dashboard_truth.py
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-04
must_haves:
  truths:
    - "Operators can tell whether a theta/order row is broker combo truth, paper strategy truth, or ledger-rebuilt fallback."
    - "Order lifecycle and position panels show combo spread value/cost basis in spread-aware terms, not misleading single-leg assumptions."
    - "Broker/ledger mismatch or degraded fallback state is visible instead of hidden."
  artifacts:
    - path: ui/dashboard.py
      provides: "Truth-source labels, combo summaries, and fallback mismatch messaging in the options lifecycle panel"
      contains: "broker_combo"
    - path: core/dashboard_positions.py
      provides: "Spread-aware display helpers reused by dashboard combo truth views"
      contains: "estimate_options_order_unrealized"
    - path: tests/test_options_combo_dashboard_truth.py
      provides: "Regression coverage for broker_combo/paper_strategy/ledger_rebuilt labeling"
      contains: "test_dashboard_labels_combo_truth_sources"
  key_links:
    - from: ui/dashboard.py
      to: core/dashboard_positions.py
      via: "spread-aware valuation and truth-source-specific display logic"
      pattern: "estimate_options_order_unrealized"
    - from: ui/dashboard.py
      to: exported orders JSON
      via: "truth_source/combo_legs fields shown to the operator"
      pattern: "truth_source|combo_legs"
---

<objective>
Make dashboard/operator surfaces truthful once combo execution exists by labeling combo truth sources explicitly and showing spread-aware values without disguising degraded fallbacks as broker fills.

Purpose: Preserve operator trust so the dashboard reflects the same execution truth as the runtime.
Output: Dashboard truth-source UI, combo spread display helpers, mismatch messaging, and targeted UI regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/debug/theta-dashboard-unrealized-pnl.md
@core/dashboard_positions.py
@ui/dashboard.py
@tests/test_dashboard_positions.py
@tests/test_options_order_lifecycle_dashboard.py

<interfaces>
From core/dashboard_positions.py:
```python
def estimate_options_order_unrealized(order_row, open_pos, *, live_premium=0.0, current_spot=0.0, current_iv=0.0, dte_years=0.0) -> dict | None
```

From ui/dashboard.py:
```python
order_files = list(orders_path.glob("OPTIONS_*_orders.json"))
rebuilt_orders = rebuild_options_orders_from_ledger(ol)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dashboard truth-label regressions for combo, paper, and ledger fallback rows</name>
  <files>tests/test_dashboard_positions.py, tests/test_options_order_lifecycle_dashboard.py, tests/test_options_combo_dashboard_truth.py</files>
  <behavior>
    - Test 1: `broker_combo` rows render a combo-specific truth label and do not show the paper-theta disclaimer.
    - Test 2: `paper_strategy` theta rows keep the existing paper-estimate disclaimer.
    - Test 3: `ledger_rebuilt` rows show a degraded/fallback caption or mismatch status.
    - Test 4: combo rows display spread-aware current value/unrealized PnL using persisted combo metadata rather than a single-leg formula.
  </behavior>
  <action>Create `tests/test_options_combo_dashboard_truth.py` and extend the existing dashboard test files to assert explicit truth labeling for `broker_combo`, `paper_strategy`, and `ledger_rebuilt`. Include a regression proving the lifecycle table shows combo leg summary and spread-aware price/current value fields for broker combo rows, while fallback rows visibly declare degraded truth. Reuse the theta-dashboard bug context so the tests block any return to misleading single-leg or underlying-price-based combo valuation.</action>
  <verify>
    <automated>python3 -m pytest tests/test_dashboard_positions.py tests/test_options_order_lifecycle_dashboard.py tests/test_options_combo_dashboard_truth.py -v</automated>
  </verify>
  <done>Failing UI truth tests exist for the exact broker/paper/ledger distinctions the operator must see.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement combo truth-source UI and spread-aware lifecycle display</name>
  <files>core/dashboard_positions.py, ui/dashboard.py</files>
  <action>Update the dashboard surfaces to consume the combo metadata introduced earlier. In `ui/dashboard.py`, add visible truth-source rendering for `broker_combo`, `paper_strategy`, and `ledger_rebuilt`; show combo leg summaries; and render a degraded/fallback caption whenever live orders are being rebuilt from ledger or otherwise disagree with broker combo truth. Keep the existing paper-theta informational note only for `paper_strategy`/`ledger_rebuilt` rows; do not show it for `broker_combo`. In `core/dashboard_positions.py`, extend the shared valuation helpers so combo rows use spread-aware current value/cost-basis logic from combo metadata or recovered spread details, never a single-leg premium formula. Preserve current rounded numeric formatting and truthful-notice style from the recent theta PnL fix.</action>
  <verify>
    <automated>python3 -m pytest tests/test_dashboard_positions.py tests/test_options_order_lifecycle_dashboard.py tests/test_options_combo_dashboard_truth.py -v</automated>
    <automated>python3 -m pytest tests/ -v</automated>
  </verify>
  <done>The options lifecycle panel tells operators exactly what truth source they are seeing and values combo rows with spread-aware logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle export → dashboard UI | Serialized combo truth becomes operator-visible supervision data |
| degraded recovery state → operator decision-making | Hidden fallback state could mislead real trading actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1000-10 | I | `ui/dashboard.py` truth display | mitigate | Show explicit `broker_combo` / `paper_strategy` / `ledger_rebuilt` labels and degraded captions instead of ambiguous rows |
| T-1000-11 | T | combo valuation display | mitigate | Reuse spread-aware valuation helpers and tests so broker combo rows cannot regress to single-leg/underlying-based math |
| T-1000-12 | R | fallback/mismatch visibility | mitigate | Surface rebuild/mismatch state in the lifecycle panel whenever ledger fallback is used for live combo supervision |
</threat_model>

<verification>
- `python3 -m pytest tests/test_dashboard_positions.py tests/test_options_order_lifecycle_dashboard.py tests/test_options_combo_dashboard_truth.py -v`
- `python3 -m pytest tests/ -v`
</verification>

<success_criteria>
- Dashboard labels broker combo, paper strategy, and ledger-rebuilt truth distinctly.
- Combo rows use spread-aware valuation and cost basis.
- Degraded fallback state is visible to the operator.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-implement-truthful-live-theta-execution-and-complex-order-or/1000-implement-truthful-live-theta-execution-and-complex-order-or-04-SUMMARY.md`
</output>
