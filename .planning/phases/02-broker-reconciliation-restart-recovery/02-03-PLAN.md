---
phase: 02-broker-reconciliation-restart-recovery
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - core/order_management/order_manager.py
  - strategies/futures/monitor.py
  - strategies/options/live_options_squeeze_monitor.py
  - tests/test_order_lifecycle/test_callback_gap_recovery.py
  - tests/test_order_lifecycle/test_duplicate_submit_prevention.py
  - tests/test_order_lifecycle/test_lifecycle_audit_transitions.py
autonomous: true
requirements:
  - RECN-01
  - RECN-02
  - RECN-03
must_haves:
  truths:
    - "When callback gaps happen, futures/options reconcile broker truth before cancel/retry and do not resubmit while a recovered active order is still unresolved."
    - "Recovered or refreshed active orders prevent duplicate entry/exit submission until the original lifecycle order reaches a terminal state."
    - "Regression coverage proves callback gaps, restart recovery, and duplicate-submit prevention stay locked across futures/options runtime paths."
    - "Blocked resubmits and recovery-driven state changes remain auditable with timestamp, source, and reason."
  artifacts:
    - path: tests/test_order_lifecycle/test_callback_gap_recovery.py
      provides: "Cross-runtime callback-gap recovery regression coverage"
      contains: "test_options_timeout_reconciles_before_retry"
    - path: tests/test_order_lifecycle/test_duplicate_submit_prevention.py
      provides: "Duplicate-submit prevention coverage for recovered active orders"
      contains: "test_futures_recovered_active_order_blocks_duplicate_entry"
    - path: core/order_management/order_manager.py
      provides: "Blocking-active-order guard and audit support for recovery safety"
      contains: "has_unresolved_active_orders"
    - path: strategies/options/live_options_squeeze_monitor.py
      provides: "Options timeout/retry guard that reconciles before cancel or resend"
      contains: "refresh_live_orders"
  key_links:
    - from: strategies/options/live_options_squeeze_monitor.py
      to: core/order_management/order_manager.py
      via: "timeout refresh resolves broker truth before retry/cancel"
      pattern: "refresh_live_orders|reconcile_trade_snapshot|has_unresolved_active_orders"
    - from: strategies/futures/monitor.py
      to: core/order_management/order_manager.py
      via: "entry/exit submit path checks blocking recovered active orders before place_order"
      pattern: "_execute_trade|_submit_order_via_manager|has_unresolved_active_orders"
    - from: tests/test_order_lifecycle/test_duplicate_submit_prevention.py
      to: strategies/futures/monitor.py
      via: "recovered active-order guard assertions"
      pattern: "active_order_unreconciled|duplicate_submit_prevented"
---

<objective>
Lock callback-gap recovery and duplicate-submit prevention with runtime safety gates and regression coverage across futures and options.

Purpose: Finish Phase 2 by proving the system will reconcile broker truth before retrying and will refuse duplicate submissions while recovered active execution is still unresolved.
Output: Safety-gate code in the shared manager and both monitors, plus callback-gap / duplicate-submit regression suites and final audit-provenance assertions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-broker-reconciliation-restart-recovery/02-RESEARCH.md
@.planning/phases/02-broker-reconciliation-restart-recovery/02-VALIDATION.md
@RULES.md
@docs/shioaji_訂單生命週期.md
@core/order_management/order_manager.py
@strategies/futures/monitor.py
@strategies/options/live_options_squeeze_monitor.py
@tests/test_order_lifecycle/test_restart_recovery_futures.py
@tests/test_order_lifecycle/test_restart_recovery_options.py

<interfaces>
Expected from Plans 01-02:
```python
def reconcile_trade_snapshot(..., source: str, reason: str): ...
def reconcile_broker_state(open_orders=None, filled_trades=None, source: str = "", reason: str = ""): ...
def restore_orders(serialized_orders): ...
```

High-risk runtime hooks:
```python
# strategies/futures/monitor.py
def _execute_trade(self, signal, price, ts, lots, *, stop_loss=None, break_even_trigger=None, trail_points=None, reason=None): ...
def _submit_order_via_manager(self, signal, price, ts, lots, ...): ...

# strategies/options/live_options_squeeze_monitor.py
def refresh_live_orders(self): ...
def submit_live_entry(self, side, signal, retries): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Run mandatory full-suite preflight before safety-gate changes</name>
  <read_first>
    - RULES.md
    - .planning/phases/02-broker-reconciliation-restart-recovery/02-VALIDATION.md
  </read_first>
  <files>none</files>
  <action>Run `python3 -m pytest tests/ -v` before adding callback-gap and duplicate-submit safety gates. If the suite is already failing, stop and record the baseline first.</action>
  <acceptance_criteria>
    - `python3 -m pytest tests/ -v` exits 0, or execution stops with the failing baseline recorded before code changes begin.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/ -v</automated>
  </verify>
  <done>The safety baseline is known before runtime gate logic changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-runtime regression coverage for callback gaps and duplicate-submit prevention</name>
  <read_first>
    - strategies/futures/monitor.py
    - strategies/options/live_options_squeeze_monitor.py
    - tests/test_order_lifecycle/test_lifecycle_audit_transitions.py
    - .planning/phases/02-broker-reconciliation-restart-recovery/02-RESEARCH.md
    - .planning/phases/02-broker-reconciliation-restart-recovery/02-VALIDATION.md
  </read_first>
  <files>tests/test_order_lifecycle/test_callback_gap_recovery.py, tests/test_order_lifecycle/test_duplicate_submit_prevention.py, tests/test_order_lifecycle/test_lifecycle_audit_transitions.py</files>
  <behavior>
    - Test 1: options timeout handling calls broker reconciliation first; if broker still reports `Submitted`/`PartFilled`, it keeps the original pending order and skips cancel/retry.
    - Test 2: futures restart with a recovered active order blocks a new entry submit and records an audit/signal reason instead of calling `place_order`.
    - Test 3: duplicate broker callbacks or replayed refresh results do not mint a second fill application or second replacement order.
    - Test 4: blocked duplicate-submit and callback-gap reconcile paths append normalized audit provenance with `timestamp`, `source`, and `reason`.
  </behavior>
  <action>Create `tests/test_order_lifecycle/test_callback_gap_recovery.py` and `tests/test_order_lifecycle/test_duplicate_submit_prevention.py`, and extend `tests/test_order_lifecycle/test_lifecycle_audit_transitions.py` for safety-gate provenance. Cover one options callback-gap path where timeout previously cancelled/retried blindly, one futures restart path where a recovered active order would otherwise allow duplicate entry, and one duplicate-callback/replay path proving deal dedupe remains intact after restart. Assert no second broker `place_order` call occurs while a recovered active order is unresolved, and assert audit rows capture reasons such as `callback_gap_refresh`, `active_order_unreconciled`, or an equivalent explicit guard code.</action>
  <acceptance_criteria>
    - `grep -n "test_options_timeout_reconciles_before_retry" tests/test_order_lifecycle/test_callback_gap_recovery.py` returns a match.
    - `grep -n "test_futures_recovered_active_order_blocks_duplicate_entry" tests/test_order_lifecycle/test_duplicate_submit_prevention.py` returns a match.
    - `grep -n "active_order_unreconciled\\|callback_gap_refresh" tests/test_order_lifecycle/test_duplicate_submit_prevention.py tests/test_order_lifecycle/test_callback_gap_recovery.py tests/test_order_lifecycle/test_lifecycle_audit_transitions.py` returns assertions or fixtures.
    - `python3 -m pytest tests/test_order_lifecycle/test_callback_gap_recovery.py tests/test_order_lifecycle/test_duplicate_submit_prevention.py tests/test_order_lifecycle/test_lifecycle_audit_transitions.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_callback_gap_recovery.py tests/test_order_lifecycle/test_duplicate_submit_prevention.py tests/test_order_lifecycle/test_lifecycle_audit_transitions.py --collect-only -q</automated>
    <automated>python3 -m pytest tests/ -v</automated>
  </verify>
  <done>The final Phase 2 safety behaviors are regression-defined before runtime gates are changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce callback-gap reconciliation and unresolved-active-order submit guards</name>
  <read_first>
    - core/order_management/order_manager.py
    - strategies/futures/monitor.py
    - strategies/options/live_options_squeeze_monitor.py
    - tests/test_order_lifecycle/test_callback_gap_recovery.py
    - tests/test_order_lifecycle/test_duplicate_submit_prevention.py
  </read_first>
  <files>core/order_management/order_manager.py, strategies/futures/monitor.py, strategies/options/live_options_squeeze_monitor.py</files>
  <action>Implement final RECN-01/02/03 safety gates. In `core/order_management/order_manager.py`, add explicit queries such as `has_unresolved_active_orders(...)` and/or `get_recovery_blockers(...)` that detect recovered or refreshed active orders still awaiting broker resolution, and log normalized audit entries when those guards block a submit. In `strategies/futures/monitor.py`, call that guard before new BUY/SELL/EXIT submissions in `_execute_trade` / `_submit_order_via_manager`; if a recovered active order is unresolved, skip `self.client.place_order(...)`, preserve the original lifecycle order, and emit a signal-audit reason like `active_order_unreconciled` instead of retrying. In `strategies/options/live_options_squeeze_monitor.py`, rework `refresh_live_orders()` so timeout handling first calls broker refresh/reconcile and only cancels or retries if the reconciled lifecycle order is truly terminal or missing; if broker truth still shows `Submitted`/`PartFilled`, keep `pending_entry` or `pending_exit_trade` attached to the existing `order_id` and do not resubmit. Ensure blocked duplicate submits and callback-gap reconciliations append normalized audit rows with explicit `source` and `reason` so operators can review why automation held the order instead of retrying.</action>
  <acceptance_criteria>
    - `grep -n "def has_unresolved_active_orders\\|def get_recovery_blockers" core/order_management/order_manager.py` returns a guard definition.
    - `grep -n "active_order_unreconciled\\|duplicate_submit_prevented" strategies/futures/monitor.py strategies/options/live_options_squeeze_monitor.py core/order_management/order_manager.py` returns explicit safety-gate reason codes or audit strings.
    - `grep -n "refresh_live_orders" strategies/options/live_options_squeeze_monitor.py` returns reconcile-before-retry logic.
    - `grep -n "_execute_trade\\|_submit_order_via_manager" strategies/futures/monitor.py` returns unresolved-active-order guard logic before broker submit.
    - `python3 -m pytest tests/test_order_lifecycle/test_callback_gap_recovery.py tests/test_order_lifecycle/test_duplicate_submit_prevention.py tests/test_order_lifecycle/test_lifecycle_audit_transitions.py -v` passes.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_order_lifecycle/test_callback_gap_recovery.py tests/test_order_lifecycle/test_duplicate_submit_prevention.py tests/test_order_lifecycle/test_lifecycle_audit_transitions.py -v</automated>
    <automated>python3 -m pytest tests/ -v</automated>
  </verify>
  <done>Runtime safety gates now reconcile before retrying and refuse duplicate submits while recovered active execution remains unresolved, with final provenance captured for audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| timeout/retry logic → broker submit path | Automated recovery code can accidentally submit duplicate live orders |
| recovered active-order state → strategy execution guards | Startup and callback-gap truth determines whether strategy may place a new order |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | D | futures/options submit path | mitigate | Guard new submits with `has_unresolved_active_orders(...)` so recovered active orders block duplicate broker placement |
| T-02-08 | T | options timeout retry flow | mitigate | Reconcile broker truth before cancel/retry and keep the original lifecycle order if broker still reports it working or partially filled |
| T-02-09 | R | blocked-submit / callback-gap handling | mitigate | Persist explicit audit rows for guard decisions and recovery actions, including `source`, `reason`, and timestamp |
</threat_model>

<verification>
Run the focused callback-gap + duplicate-submit suite:
- `python3 -m pytest tests/test_order_lifecycle/test_callback_gap_recovery.py tests/test_order_lifecycle/test_duplicate_submit_prevention.py tests/test_order_lifecycle/test_lifecycle_audit_transitions.py -v`
Run the mandatory repo-wide safety suite before execution starts and again after this plan completes:
- `python3 -m pytest tests/ -v`
</verification>

<success_criteria>
- Callback-gap timeout logic reconciles first and only retries when broker truth says the original order is gone or terminal.
- Recovered active orders block duplicate futures/options submits until the original lifecycle order resolves.
- Final Phase 2 regression suites prove callback gaps, restart recovery, and duplicate-submit prevention remain locked with auditable provenance.
</success_criteria>

<output>
After completion, create `.planning/phases/02-broker-reconciliation-restart-recovery/02-broker-reconciliation-restart-recovery-03-SUMMARY.md`
</output>
