---
context: research
phase: none
task: 1
total_tasks: 3
status: paused
last_updated: 2026-04-20T13:27:03.116Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No roadmap-backed phase workflow yet — `gsd-add-phase` and `gsd-plan-phase` both stop because `.planning/ROADMAP.md` does not exist — initialize roadmap/state before trying formal GSD phase planning.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Roadmap-less GSD planning | Attempting `/gsd-add-phase` or `/gsd-plan-phase` without `.planning/ROADMAP.md` causes the workflow to block immediately. | blocking | Create the roadmap/state files first, or keep work in spike/todo mode until roadmap exists. |
| Split 8501 ownership assumptions | 8501 is code-isolated, but repo wiring is split: `autostart.sh` points 8501 at `ui/backtest_dashboard.py`, while `docker-compose.yml` points 8501 at `ui/dashboard.py`. | advisory | Before any real 8501 build, declare one runtime owner and one target app for port 8501. |
| Premature adaptive tuning | The current `logs/trade_attribution.csv` supports history review only; it lacks populated feature payloads and a usable score field for threshold tuning. | advisory | Finish the adaptive dataset pipeline before implementing score-based tuning UI. |

<current_state>
Adaptive-workbench feasibility spike is complete and paused. The repo now has `.planning/spikes/` artifacts proving that 8501 can host an isolated review-first console, but formal roadmap/phase planning is blocked by missing `.planning/ROADMAP.md`. Work is paused because the user reported small dashboard issues and wants to switch attention there next.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Create and run 8501 feasibility spikes - Done
  - `001-tuning-workbench-ia`: PARTIAL, code-level isolation from `main.py` and 8500 confirmed
  - `002-adaptive-loop-mvp`: PARTIAL, review slices work but adaptive dataset is too sparse for score tuning
  - `003-tuning-panel-shape`: PARTIAL, standalone Streamlit prototype booted successfully
- Task 2: Capture adaptive dataset follow-up - Done
  - `.planning/todos/pending/adaptive-trade-dataset-pipeline.md` created
- Task 3: Formal GSD phase planning - Blocked before start
  - Could not proceed because `.planning/ROADMAP.md` is missing
</completed_work>

<remaining_work>

- Triage the newly reported dashboard issue
- Decide whether the problem is in the live dashboard (`ui/dashboard.py`) or the 8501 backtest dashboard (`ui/backtest_dashboard.py`)
- If returning to adaptive work later:
  - initialize `.planning/ROADMAP.md` / `.planning/STATE.md`
  - add a formal phase for the 8501 review-first console
  - keep score-tuning scope gated behind the adaptive dataset pipeline
</remaining_work>

<decisions_made>

- Decided to keep adaptive-console exploration completely isolated from `main.py` and the 8500 live dashboard because the user made that a hard constraint and the spike showed it is feasible.
- Decided not to treat current `trade_attribution.csv` as ready for adaptive tuning because it only contains sparse `dummy` / `NEUTRAL` rows with empty feature payloads.
- Decided to pause here instead of forcing roadmap work because dashboard issues became the higher-priority interruption.
</decisions_made>

<blockers>
- No `.planning/ROADMAP.md`: blocks formal `gsd-add-phase` / `gsd-plan-phase`
- Dashboard issue details are not recorded yet: exact page, error, and repro steps are still missing
</blockers>

## Required Reading (in order)
1. `RULES.md` — safety constraints for any trading-system code change
1. `.planning/.continue-here.md` — current paused context and blockers
1. `.planning/HANDOFF.json` — machine-readable paused state
1. `.planning/spikes/MANIFEST.md` — summary of 8501 adaptive-workbench spike outcomes
1. `.planning/todos/pending/adaptive-trade-dataset-pipeline.md` — next prerequisite if adaptive work resumes
1. `docs/repo-history.md` — canonical history/context for recent infra and documentation work

## Critical Anti-Patterns (do NOT repeat these)
- ROADMAP-less planning: invoking formal GSD phase workflows without `.planning/ROADMAP.md` → initialize roadmap/state first
- Split 8501 ownership: assuming port 8501 already has one authoritative app/owner → confirm whether autostart or docker-compose is the intended authority before implementation
- Premature tuning UI: building score-threshold controls before the dataset contains real feature/score coverage → finish dataset pipeline first

## Infrastructure State
- `main.py`: unchanged during the spike
- `ui/dashboard.py` (live dashboard / 8500): untouched by the spike
- `ui/backtest_dashboard.py` (backtest dashboard / 8501): inspected only, not modified
- No background processes from the spike are meant to remain running
- Worktree is dirty beyond the spike files; do not assume a clean branch

## Pre-Execution Critique Required
- Design artifact: `.planning/spikes/MANIFEST.md`
- Critique focus: if adaptive work resumes, verify that scope stays review-first and does not smuggle in live-path changes or score tuning without data coverage
- Gate: Do NOT begin full implementation until dashboard interruption is triaged and roadmap initialization is resolved

<context>
The spike answered the core feasibility question: 8501 is a good place for a review-first adaptive console, but not yet for real tuning. The main unresolved thread is now the dashboard issue the user just raised. Resume by investigating that dashboard problem first, not by continuing the adaptive roadmap thread blindly.
</context>

<next_action>
Start with: ask the user which dashboard is affected, what symptom they see, and how to reproduce it; then investigate that path before resuming adaptive-console planning.
</next_action>
